services:
  file-integrity-monitor:
    build: .
    container_name: file-integrity-monitor
    volumes:
      - ./monitored:/app/monitored
      - ./reports:/app/reports
    environment:
      - MONITORED_PATH=/app/monitored
      - REPORTS_PATH=/app/reports